import React from 'react'
import { Text, View, Image, StyleSheet, Dimensions, Button, TouchableWithoutFeedback } from "react-native";
import { ip, port } from "../utils"

export default function PhotoViewer({ navigation }) {

    //state = {
        //image: null,
    //};

    //onPhotoOpen = (image) => {
        //this.setState({image});
    //};

    //this.state;
    //if (gallery) {
        //return <Image style={{width: 300, height: 200}} source={{uri: image.uri}}/>
    //}

    //return this.props.renderContent({onPhotoOpen: this.onPhotoOpen});

    return(
        <Text>Photo viewer here.</Text>
    )
}